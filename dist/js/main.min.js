// Add any necessary JS functions here, such as for ScrollReveal

document.addEventListener("DOMContentLoaded", function() {
    // ScrollReveal example
    ScrollReveal().reveal('.is-revealing', { delay: 200 });
});

document.addEventListener("DOMContentLoaded", function () {
    const features = document.querySelectorAll(".feature-extended");

    const revealOnScroll = () => {
        let windowHeight = window.innerHeight;
        features.forEach((feature) => {
            let elementTop = feature.getBoundingClientRect().top;
            if (elementTop < windowHeight - 100) {
                feature.classList.add("reveal");
            }
        });
    };

    window.addEventListener("scroll", revealOnScroll);
    revealOnScroll(); // Trigger once on load
});

document.addEventListener("DOMContentLoaded", function () {
    const features = document.querySelectorAll(".feature-extended");

    const revealOnScroll = () => {
        let windowHeight = window.innerHeight;
        features.forEach((feature) => {
            let elementTop = feature.getBoundingClientRect().top;
            if (elementTop < windowHeight - 100) {
                feature.classList.add("reveal");
            }
        });
    };

    window.addEventListener("scroll", revealOnScroll);
    revealOnScroll(); // Run on page load
});
